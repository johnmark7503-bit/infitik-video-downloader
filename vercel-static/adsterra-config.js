/**
 * Paste the values from the exact Native Banner and Social Bar codes generated
 * by your Adsterra dashboard, then change enabled to true.
 */
window.INFITIK_ADS = Object.freeze({
  enabled: false,
  nativeBanner: Object.freeze({
    scriptSrc: "",
    containerId: ""
  }),
  socialBar: Object.freeze({
    scriptSrc: ""
  })
});
